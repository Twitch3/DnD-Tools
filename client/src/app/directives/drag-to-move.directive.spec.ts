import { DragToMoveDirective } from './drag-to-move.directive';

describe('DragToMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new DragToMoveDirective();
    expect(directive).toBeTruthy();
  });
});
