import { DragToRotateDirective } from './drag-to-rotate.directive';

describe('DragToRotateDirective', () => {
  it('should create an instance', () => {
    const directive = new DragToRotateDirective();
    expect(directive).toBeTruthy();
  });
});
